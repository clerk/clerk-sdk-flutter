name: clerk_test
description: "Test support modules for clerk_auth and clerk_flutter"
publish_to: 'none'
version: 0.0.12-beta

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  clerk_auth:
    path: ../clerk_auth
  dart_dotenv: ^1.0.1
  http: ^1.1.0
  test: ^1.25.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
